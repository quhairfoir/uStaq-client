import React from 'react'
import {Row, Col, PageHeader, Table, Form, FormGroup, Button, ControlLabel, Checkbox, FormControl} from 'react-bootstrap'
import {Route, Switch, Link} from 'react-router-dom'


class SignIn extends React.Component {



  render() {
    return (
      <div>
      
      </div>
    )
  }
}

export default SignIn

//whitelist localhost 
//cors - many apis don't allow to share info with two endpoints, like google oauth, do it from server instead of localhost
// go to developer and add localhost as a domain allowed to process the info 
//firebase newest version